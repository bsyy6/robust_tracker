#include <gtest/gtest.h>
#include <robust_follower/robust_follower.h>

TEST (robust_follower_LibraryTests, Print) {
    EXPECT_EQ(2, 2);
}

TEST (robust_follower_LibraryTests, Calculation) {
    EXPECT_EQ(4, 2*1);
}